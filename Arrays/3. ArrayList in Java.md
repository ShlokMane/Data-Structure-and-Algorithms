Advantages of Array List over normal array -

1. Dynamic Size
2. Rich Library Functions

In real life situation we don't know the size of the data structure we want as the requirement can change in future and
hence having a dynamic size array is always better than fixed size.

ArrayList contains a lot of function which normal array doesn't have.

```java
import java.util.*;

class Test {
    static void main(String[] args) {
        ArrayList<Integer> arrayList = new ArrayList<>();
        arrayList.add(10);
        arrayList.add(20);
        arrayList.add(30);
        System.out.println(arr);
        System.out.println(arrayList.contains(20));
        System.out.println(arrayList.getFirst());
    }
}
```
